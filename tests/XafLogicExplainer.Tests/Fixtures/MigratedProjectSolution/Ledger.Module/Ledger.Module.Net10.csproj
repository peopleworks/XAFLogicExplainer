<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Fixture only. Never built. The project file the application is actually built from, beside the
    pre-SDK one it replaced. It compiles every source file in the folder.
  -->

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <DevExpressVersion>26.1.3</DevExpressVersion>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="DevExpress.ExpressApp.Xpo" Version="$(DevExpressVersion)" />
    <PackageReference Include="DevExpress.Persistent.BaseImpl.Xpo" Version="$(DevExpressVersion)" />
  </ItemGroup>

</Project>
